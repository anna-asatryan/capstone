Optional frozen participant exports can live here later.

The official deterministic path does not require them.

If you add canonical exports in the future, use stable filenames such as:

- `participants.csv`
- `trials.csv`
- `quiz_responses.csv`
